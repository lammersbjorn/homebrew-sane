# Homebrew Tap For Sane

Install the beta:

```bash
brew tap lammersbjorn/sane
brew install sane
```

Then run:

```bash
sane status
```

This tap tracks prerelease Sane artifacts before the stable `v1` release.
